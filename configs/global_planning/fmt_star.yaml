algorithm: fmt_star
category: global_planning
params:
  - name: num_samples
    type: int
    default: 1500
    min: 1
    max: 200000
    description: 단일 배치로 뿌리는 free 샘플 수 (start/goal 제외).
  - name: gamma
    type: float
    default: 30.0
    min: 0.01
    max: 1000.0
    description: marching 연결 반경 계수 γ. r_n = γ·(log n / n)^(1/2). (Janson et al. 2015)
  - name: seed
    type: int
    default: 1
    min: 0
    max: 2147483647
    description: 난수 시드 (재현성).
